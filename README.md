# 1-Monthon26s12

## テスト用のサーバーについて

起動手順

1)dockerを開く

2)以下のコマンドを打ってサーバーのディレクトリに移動する
```
cd server
```
必要に応じてlsコマンドで現在位置を確認する

3)以下のコマンドを打つ
```
docker compose up -d
```

停止させる際は以下のコマンドを打つ
```
docker compose down
```